#include <stdlib.h>
#include <stdio.h>

/**
 * sub - this subtracts two integers.
 * @a: first unit 
 * @b: second unit
 * Return: subtract final result
 */
int sub(int a, int b)
{
	return (a - b);
}
